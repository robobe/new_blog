{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug toolchain_demo",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/toolchain_demo",
            "args": ["Ada"],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "MIMode": "gdb",
            "preLaunchTask": "CMake: build"
        }
    ]
}
